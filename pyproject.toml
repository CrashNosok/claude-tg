[project]
name = "claude-tg"
version = "0.1.0"
description = "Telegram bridge to a Claude Code session running in tmux"
requires-python = ">=3.12"
dependencies = ["python-telegram-bot[job-queue,rate-limiter]>=21"]
